# backend/docker-compose.yml
version: "3.9"
services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 5s
      retries: 10

  neo4j:
    image: neo4j:5-community
    environment:
      NEO4J_AUTH: neo4j/${NEO4J_PASSWORD}
    volumes:
      - neo4j-data:/data
    ports:
      - "7474:7474"
      - "7687:7687"

  keycloak:
    image: quay.io/keycloak/keycloak:25.0
    command: start-dev --import-realm
    environment:
      KEYCLOAK_ADMIN: ${KEYCLOAK_ADMIN_USER}
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD}
      KC_DB: postgres
      KC_DB_URL: ${SUPABASE_DB_URL_JDBC}
      KC_DB_SCHEMA: keycloak
      KC_DB_USERNAME: ${SUPABASE_DB_USERNAME}
      KC_DB_PASSWORD: ${SUPABASE_DB_PASSWORD}
    volumes:
      - ./keycloak/realm-export.json:/opt/keycloak/data/import/realm-export.json
    ports:
      - "8080:8080"

#  sentinel-api:
#    build:
#      context: .
#      dockerfile: Dockerfile
#    environment:
#      ConnectionStrings__Supabase: ${SUPABASE_DB_URL_DOTNET}
#      Keycloak__Authority: "http://keycloak:8080/realms/sentinel"
#      Keycloak__Audience: "sentinel-api"
#    depends_on:
#      keycloak:
#        condition: service_started
#    ports:
#      - "5000:5000"

volumes:
  neo4j-data:
